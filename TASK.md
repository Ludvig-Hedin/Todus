# Tasks

**Moved.** This file was a single growing sprint log. Its contents were split on
2026-07-25 into the two homes that actually get worked:

| What | Where |
|------|-------|
| Code / agent follow-up work | [`backlog/`](backlog/README.md) — `bun backlog:check` |
| Work only the owner can do (env, dashboards, signing, accounts) | [`user-tasks/`](user-tasks/README.md) — `bun user-tasks:check` |
| What already shipped | [`changelog/`](changelog/README.md) |

All 270 items were migrated verbatim (8 open, 262 archived as done), plus the "Manual
Inputs Required" section, which became user tasks. Items carry `source: TASK.md` in their
front matter. The full original remains in Git history.
