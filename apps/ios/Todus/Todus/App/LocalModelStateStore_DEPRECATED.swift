// Superseded by Services/AI/Local/LocalModelStateStore.swift (full @Observable
// implementation with LocalModelInstallState enum + ModelDownloadService wiring).
// This file is intentionally empty to avoid a duplicate filename / duplicate
// class-name collision in the Todus target. Safe to delete from disk once the
// Xcode project no longer references the old path. Kept for one release cycle
// to ease bisecting if anything in `App/` was importing from the old location.
